<!-- category widget -->

@*<div class="aside-widget">
    <div class="section-title">
        <h2 class="title">DANH MỤC</h2>
    </div>
    <div class="category-widget" ng-controller="_DanhMucPost">
        <ul>
            <li ng-repeat="item in listDanhMuc"><a href="#">{{item.TenDM}}<span>{{item.LuotXem}}</span></a></li>
        </ul>
    </div>
</div>*@

<div class="aside-widget">
    <div class="section-title">
        <h2 class="title">DANH MỤC</h2>
    </div>
    <div class="category-widget" ng-controller="getMenu">
        <ul>
            <li ng-repeat="item in menus"style=" border-bottom: none;padding: 5px 0px;">
                <a href="#">{{item.TenDM}}</a>
                <div class="dropdown">
                    <div class="dropdown-body">
                        <ul class="dropdown-list" style="font-size: 12px;">
                            <li ng-repeat="key in item.children" style=" padding: 0px 0px;">
                                <a style=" padding: 5px 15px;" onclick="setTimeout(function () { window.location.reload() }, 100)" href="/baiViet/baiviettheoloai#!?maLoai={{key.madmc}}">
                                    <i class="fas fa-angle-right"></i> {{key.tendmc}}<span>{{key.luotXem}}</span>
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>
            </li>
        </ul>
    </div>
</div>

@*<div id="nav-bottom" ng-controller="getMenu">
    <div class="container">
        <ul class="nav-menu">
            <li class="has-dropdown" ng-repeat="item in menus">
                <a href="#">{{item.TenDM}}</a>
                <div class="dropdown">
                    <div class="dropdown-body">
                        <ul class="dropdown-list">
                            <li ng-repeat="key in item.children">
                                <a onclick="setTimeout(function () { window.location.reload() }, 100)" href="/baiViet/baiviettheoloai#!?maLoai={{key.madmc}}">
                                    {{key.tendmc}}
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>
            </li>
        </ul>
    </div>
</div>*@
<!-- /category widget -->
